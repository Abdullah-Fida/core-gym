# PRODUCTION — used by `npm run build` and Vercel
# Set this in Vercel Dashboard > Settings > Environment Variables
# The Render service was deleted (returns x-render-routing: no-server).
# The backend now runs as its own Vercel project from the core-gym-backend repo.
VITE_API_URL=https://core-gym-backend.vercel.app/api
